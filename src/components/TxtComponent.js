import { Text } from "react-native-elements";
import { View } from "react-native";

export default function TxtComponent({Texto}) {
  return (
    <View>
      <Text>{Texto}</Text>
    </View>
  );
}
