import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignContent: "center",
    alignItems: "center",
    backgroundColor: "#fff",
  },

  name: {
    fontSize: 30,
    textAlign: "center",
  },

  adress: {
    fontSize: 25,
    color: "#add8e6",
    textAlign: "center",
  },

  description: {
    fontSize: 20,
    color: "#a9a9a9",
    textAlign: "center",
  },

  imgmain: {
    width: 500,
    height: 500,
    alignSelf: "center",
  },
});
