import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignContent: "center",
    backgroundColor: "#fff",
  },

  name: {
    fontSize: 30,
  },

  location: {
    fontSize: 25,
    color: "#add8e6",
  },

  description: {
    fontSize: 20,
    color: "#a9a9a9",
  },

  img: {
    width: 500,
    height: 500,
  },
});
